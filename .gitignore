# Never commit credentials. jackal keeps them under ~/.jackal/ (outside the
# repo), but this guards against a stray copy landing here.
*.env

# Python
__pycache__/
*.py[cod]

# Subagent-driven-development scratch workspace (ledgers, briefs, reports)
.superpowers/
*/superpowers/